# Example environment variables for local development (Vite)
# Replace values with your API key and desired model.
# If you want to use Gemini (Google) set the model to a Gemini model like `gemini-2.5-flash`.
# To use GPT-5 mini you would set `VITE_AI_MODEL=gpt-5-mini` but you must also switch the chat client to OpenAI.

VITE_API_KEY=your_gemini_api_key_here
VITE_AI_MODEL=gemini-2.5-flash
VITE_N8N_WEBHOOK_URL=https://n8n-webhook-url
